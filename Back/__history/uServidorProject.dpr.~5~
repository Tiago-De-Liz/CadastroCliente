program uServidorProject;
{$APPTYPE GUI}

uses
  Vcl.Forms,
  uSServidor in 'Fontes\uSServidor.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFormPrincipal, Form1);
  Application.Run;
end.
