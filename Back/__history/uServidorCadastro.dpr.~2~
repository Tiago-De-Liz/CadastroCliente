program uServidorCadastro;

uses
  Vcl.Forms,
  uSServidor in 'Fontes\uSServidor.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
